import 'package:flutter/material.dart';

class OneClass extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
 
 //Return whatever widget you want to return to the main list from here
    return Center(
      child: Container(
        child: Text('Empty Body 0'),
      ),
    );
  }

}
